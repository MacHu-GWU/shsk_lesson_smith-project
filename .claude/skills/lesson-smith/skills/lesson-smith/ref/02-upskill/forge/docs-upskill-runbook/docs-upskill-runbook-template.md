# [Course Name] Runbook

> Everything you need to get this course running: what to set up before you start, and the commands, environment settings, and manual steps that come up along the way. When something will not run, start here.

## 1. Environment Setup

[The one-time setup that comes before the first task. Most of these commands already live in mise.toml; copy them verbatim.]

- Prerequisites: [tools and versions, from [mise.toml](../../mise.toml)]
- First-time setup:
1. [a command, exactly as you would type it]
2. [a command]

## 2. Operations Along the Way

[Do not walk through the tasks one at a time. A sentence on the general pattern is enough, followed by the handful of places that break it. Spell out the steps an experienced hand performs without thinking, since those are the ones a student stalls on.]

General pattern: [one line, for example: most tasks are read the README and follow along, with nothing extra to run; only the places below need more.]

Places that need more:

- In [examples/0X]: [the extra step, for example signing up for an account, turning on a setting in the browser, changing into a particular directory, exporting an environment variable]
- In [examples/0Y]: [the extra step]

## 3. Common Snags

[Known footguns and how to get past them. If nobody has hit one yet, say so and fill this in as they come up.]
